[package]
name = "rrust-test"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
rrust = { path = "../rrust" }

[dev-dependencies]
trybuild = "1.0"
